spring:
  datasource:
    url: "jdbc:mysql://\${DB_HOST:mysql-1-service}:3306/\${DB_NAME:p_14054_2_prod}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul"
    username: "\${DB_USERNAME:p14054}"
    password: "\${DB_PASSWORD}"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: false
  data:
    redis:
      host: "\${REDIS_HOST:redis-1-service}"
      port: "\${REDIS_PORT:6379}"
      password: "\${REDIS_PASSWORD}"
  session:
    store-type: redis
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    com.ll.p14054: INFO

custom:
  gen:
    dir-path: /gen
